{
  "name": "echo-backup-coordinator",
  "version": "1.0.0",
  "description": "Automated backup coordinator for ECHO OMEGA PRIME fleet — D1, KV, R2, Worker code archives",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.99.0",
    "@cloudflare/workers-types": "^4.20250313.0",
    "typescript": "^5.7.3"
  }
}
